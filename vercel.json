{
  "buildCommand": "pnpm --filter @oprwa/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
